import Layout from "../components/Layout";

function AddEventPage() {
  return (
    <Layout title="Add Event" description="Add a new event">
      <h1>Add event page</h1>
    </Layout>
  );
}

export default AddEventPage;
